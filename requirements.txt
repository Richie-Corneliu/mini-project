# GPU: torch from the cu121 wheel index (RTX 3050). CPU fallback: drop the
# --extra-index-url line and install plain torch>=2.0.0.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121
lap>=0.4.0

ultralytics
supervision
trackers
opencv-python
pyyaml
